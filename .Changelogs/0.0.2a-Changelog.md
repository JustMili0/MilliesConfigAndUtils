# Millie's Core Libraries - Alpha 0.0.2

- [WIP] Added events system (Fabric-like)
- Reorganized Util classes into `client`, `common` and `server`.
  - Only super-specific classes are in either `client` or `server`
  - Datagen utils can now be found in `common.datagen`
- Changed method names that in Util classes
  - I did that like a week ago and forgot to wrote down what's what but who cares at this stage of development?
- Added `AttributeUtil` 
- Added `ModUtil`
- Added `RenderStateUtil`, though it will not be available to use until 1.21.11 port of Millie's Core Libraries
- Added `ServerUtil`
- Removed `FileUtil`