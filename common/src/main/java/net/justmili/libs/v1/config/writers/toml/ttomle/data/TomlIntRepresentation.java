package net.justmili.libs.v1.config.writers.toml.ttomle.data;

public enum TomlIntRepresentation {
    DECIMAL,
    BINARY,// currently unsupported but is here for future implementation
    OCTAL,// currently unsupported but is here for future implementation
    HEXADECIMAL // currently unsupported but is here for future implementation
}

